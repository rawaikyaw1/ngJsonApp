{
  "name": "server",
  "version": "1.0.0",
  "description": "Angular CRUD with json file",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "rawai",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
